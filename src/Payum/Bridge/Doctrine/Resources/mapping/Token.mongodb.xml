<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mongo-mapping xmlns="http://doctrine-project.org/schemas/odm/doctrine-mongo-mapping"
                        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                        xsi:schemaLocation="http://doctrine-project.org/schemas/odm/doctrine-mongo-mapping
                        http://doctrine-project.org/schemas/odm/doctrine-mongo-mapping.xsd">

    <mapped-superclass name="Payum\Bridge\Doctrine\Document\Token">

      <field name="hash" type="string" id="true" strategy="NONE" />

      <!-- Is doctrine mongo odm can serialize objects like ORM? Point me how to do it -->
      <field name="detailsSerialized" type="string" />

      <field name="afterUrl" type="string" />

      <field name="targetUrl" type="string" />

      <field name="paymentName" type="string" />

    </mapped-superclass>

</doctrine-mongo-mapping>
